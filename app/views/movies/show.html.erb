<p id="notice"><%= notice %></p>
<h2><%= @movie.title %></h2>
<%= link_to 'Edit', edit_movie_path(@movie) %> |
<%= link_to 'Back', movies_path %>
  <br>
  <p>
    <strong>Category:</strong>
    <%= @movie.category.try :type %>
  </p>
  <p>
    <b>Image:</b>
    <%= link_to image_tag(@movie.movie_image.thumb.url), @movie.movie_image.medium.url %>
  </p>
  <br>
  <p><%= @movie.review %></p>
  <br>
    <% end %>
  </p>