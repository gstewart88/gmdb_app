<%= category.name %>
<%= link_to("Show #{category.name}", category_path(category)) %>
<%= button_to('Delete Category', category_path(category), method: :delete, data:{confirm: "Delete Category? Are you sure?"}) %>
<%= link_to("Edit Category", edit_category_path(category)) %>
<br>